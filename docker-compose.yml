version: '3.9'

services:
  backend-app:
    extends:
      file: backend/docker/docker-compose.yml
      service: app
  frontend:
    extends:
      file: frontend/docker-compose.yml
      service: frontend
